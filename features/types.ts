let str = 'hello'
str += 1

const today = new Date()
today.getMonth()

// today.afe --> Property 'afe' doesn't exist on type

const person = {
  age: 20
}

// person.name --> Property 'name' doesn't exist on type

class Color {

}
const red = new Color()

